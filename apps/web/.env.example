# Server configuration
PORT= 3000 # default 3000
# Localization 
# You can these from https://app.tolgee.io/
NEXT_PUBLIC_TOLGEE_API_KEY= ""
NEXT_PUBLIC_TOLGEE_API_URL= ""
TOLGEE_PROJECT_ID= ""
# Database mongodb (with replicaSet enabled*)
DATABASE_URL="mongodb://localhost:27017/website?replicaSet=rs0"
# AUTHENTICATION OPTIONS
PASSWORD_PEPPER="" # Optional - HMAC the password with a server-side secret (pepper)
# JWT configuration
# Algorithm is set to HS256 by default, you can change that in app.config.ts, then rebuild.
## HS256: require only JWT_SECRET (at least 32 random bytes)
JWT_SECRET="replace_with_long_random_secret_string"
## RS256 / EdDSA: require both keys in PEM format (multiline)
# JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
# JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"