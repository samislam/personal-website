{
  "name": "nexst",
  "devDependencies": {
    "del-cli": "^6.0.0",
    "prettier": "^3.2.5",
    "turbo": "^2.3.0",
    "typescript": "5.5.4"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.9.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "clean": "del \"./{node_modules,.turbo}\""
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
